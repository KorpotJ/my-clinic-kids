-- =====================================================================
-- ROLLBACK ของ 2026-08-20_add-uniq-child-per-session.sql
--
-- ⚠️ รันแล้วเด็กคนเดียวจะจองคาบเดียวซ้ำได้อีก
--    และโค้ด sessions-api จะกลับไปดัก constraint ที่ไม่มีอยู่จริง
--    (ไม่พัง แค่ไม่มีอะไรกัน — เหมือนสภาพก่อน 20 ส.ค.)
-- =====================================================================

BEGIN;

DROP INDEX IF EXISTS uniq_child_per_session;

COMMIT;
